#pragma once

//#include <glm/glm.hpp>
//#include  <algorithm>
//// glm::vec2 utilities
//
//static float f_min(float x1, float x2) {
//	if (x1 < x2)
//		return x1;
//	else
//		return x2;
//}
//
//static bool coord_inside_rect(glm::vec2 coord, glm::vec2 rect ) {
//	return coord.x > std::min(coord.x,rect.x) && coord.y > std::min(coord.y, rect.y) &&
//		coord.x < std::max(coord.x, rect.x) && coord.y < std::max(coord.y, rect.y);
//
//	//return coord.x > getMinX() && coord.y > getMinY() &&
//	//	coord.x < getMaxX() && coord.y < getMaxY();
//}
//
//static glm::vec2 rect_center(glm::vec2 rect) {
//	glm::vec2 center = 
//}
